# 의존성
node_modules/

# 빌드 / 렌더 산출물
out/
dist/
build/
.cache/

# 렌더된 영상·오디오 (용량이 커서 git 에 넣지 않음)
*.mp4
*.mov
*.webm
*.wav
*.mp3

# 자체 git 저장소를 가진 하위 프로젝트 (별도로 관리)
my-video/

# 머신 종속 심볼릭 링크 (.agents/skills 의 절대경로를 가리킴)
# 실제 스킬 본문은 .agents/skills/ 에 있고, 링크는 스킬 CLI 가 재생성합니다.
.claude/skills/

# 로컬 환경
.env
.env.*
!.env.example

# OS / 에디터
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
